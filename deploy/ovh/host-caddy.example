# Example only: merge this site block into the shared edge-proxy Caddyfile.
# The Movement Break app runs as a Docker Compose service attached to the
# external edge-proxy network with the movement-break alias.

movement.kevinferretti.com {
  encode zstd gzip
  reverse_proxy movement-break:8787
}
