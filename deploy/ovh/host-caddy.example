# Example only: merge this site block into the OVH host's global Caddyfile.
# The Movement Break deployment runs the app with systemd on the host and does
# not own the public reverse proxy.

movement.kevinferretti.com {
  encode zstd gzip
  reverse_proxy 127.0.0.1:8787
}
